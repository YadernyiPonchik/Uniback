info:
  name: Create User
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8000/auth/users/
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
          "email": "superworker123999898967776777@gmail.com",
          "name": "messi",
          "password": "messi1234",
          "re_password": "messi1234"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
